// This lesson introduced the logical Or operator, ||, for use when testing for either of two conditions to be met.
function testLogicalOr(val) {
  // Only change code below this line

  if (val < 10 || val > 20) {
    return "Outside";

  }

  // Only change code above this line
  return "Inside";
}

// Change this value to test
testLogicalOr(15);
