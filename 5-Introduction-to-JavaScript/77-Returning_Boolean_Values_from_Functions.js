// This lesson returned a boolean value from a function with a simple operator statement rather than use a longer If statement.
function isLess(a, b) {
  // Fix this code
   return a < b;


}

// Change these values to test
isLess(10, 15);
