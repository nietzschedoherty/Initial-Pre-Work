// This lesson introduced the logical And operator, &&, for use when needing to pass two comparisons for to return a true value.
function testLogicalAnd(val) {
  // Only change code below this line

  if (val <= 50 && val >=25) {

      return "Yes";

  }

  // Only change code above this line
  return "No";
}

// Change this value to test
testLogicalAnd(10);
