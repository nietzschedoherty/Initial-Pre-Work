// This lesson introduced the Math.random() function to return a random number between or including 0 up to, but not including 1.
function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
