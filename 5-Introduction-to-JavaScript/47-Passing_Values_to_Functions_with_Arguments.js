// This lesson showed how to define a function with two variables and then call the function by defining the variables in the call statement.
// Example
function ourFunctionWithArgs(a, b) {
  console.log(a - b);
}
ourFunctionWithArgs(10, 5); // Outputs 5

// Only change code below this line.
function functionWithArgs(a, b) {
  console.log(a+b);
}
functionWithArgs(33,57);
