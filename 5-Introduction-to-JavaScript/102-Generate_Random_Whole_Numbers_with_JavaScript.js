// This lesson introduced the Math.floor function, which rounds a number down to nearest whole number, to be used with Math.random to find a random whole number between 0 and some number.
var randomNumberBetween0and19 = Math.floor(Math.random() * 20);

function randomWholeNum() {

  // Only change code below this line.

  return Math.floor(Math.random()*10);
}
